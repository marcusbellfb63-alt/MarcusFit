# Runtime system map

Baseline: accepted MarcusFit 10.1.0 at `d66fe5cc53bca31db59ae288429bb95c16d1585c`.
Ranges are inclusive approximations from that commit. Relationships marked
“direct” are verified calls or reads in source. “Inferred” is used only where
static source cannot prove browser execution.

Abbreviations: `01`–`05` mean the corresponding file under `assets/js/`; `D`
means `day-YYYY-MM-DD`; `W` means `day-YYYY-MM-DD-wo`; `LC` means
`mf-exercise-state`; `none` means the system has no owned persistent store.

## Ownership inventory

| System | Current location | Primary functions | Globals/constants | Primary DOM IDs/sections | Storage read → write | Direct dependencies / callers | Exposure | Difficulty | Compatibility notes |
|---|---|---|---|---|---|---|---|---|---|
| Core constants/versioning | `01:3-15` | none | `APP_VERSION`, `LIFECYCLE_VERSION` | title/version labels are static HTML | none | Read by profile, proposal, backup, starter, habits, basketball; loaded first | Cross-file lexical | Low | Runtime remains 10.1.0 during 10.1.1/10.1.2. |
| Base program `P` | `01:123-239` | data only | `P` | `program-days`, day selectors (indirect) | none | Resolved-program, proposal, rendering, export, starter snapshot; consumers throughout `02`–`04` | Protected cross-file lexical | High | Byte-equivalence and stable exercise IDs are tested; never mutate. |
| Exercise/program data | `01:17-45,123-239`; starter data `04:830-965` | `getResolvedProgram`, `getResolvedDays`, `getSafeDayForLog`; `p958*` template helpers | `HABITS`, `WO_RECS`, `P`, `P958_TEMPLATES`, `P958_REGISTRY` | `program-days`, `woDaySelect`, `woDayNoteOut`, `woRecs*` | `LC`, overrides, recommendations, profile → same owning stores only | Lifecycle, personalization, program UI, logging, export, Stats | Cross-file; selected `window` template APIs | High | Large immutable datasets should move intact; resolved data is layered over `P`. |
| Exercise lifecycle/overrides | `01:241-1732`; writers `02:3238-3764`, `03:1326-1833`; validation/debug `04:553-826` | `getOvr`, `getLifecycle`, `saveLifecycle`, `ex*`, day override/add/disable helpers, `getResolvedDays`, `mfRunPostRestoreValidation` | `OVR`, `LIFECYCLE_KEY`, schemas/field allowlists | lifecycle summary/warning IDs `lc*` | `mf-overrides`, `mf-exercise-state` ↔ same | Proposal apply/undo, AI Sync, rendering, progression, export, restore; called by nearly all program systems | Public/debug plus cross-file | High | Preserve stable IDs, unknown fields, raw rollback snapshots, and `P` immutability. |
| Daily tracking | state/render `02:3-18`; form/render/save `03:1-70,447-470,1988-2331`; wrappers `04:1972-1978,2073-2135` | `setTog`, `applyStateToForm`, `loadDay`, `saveDay`, `p85ExecuteSave`, `dKey`, draft helpers | `tDate`, `toggleStates`, `habitState`, `gym`, `logGym`, `DRAFT_KEY` | `screen-log`, metrics inputs, `bm-*`, `dayNotes`, date controls, save UI | `D`, draft, recurring stores → `D`, `W`, draft, recurring events | Habits/adherence/workout/progression; History/export/Stats consume it | Inline + cross-file | High | `tDate` is mutable; habit/adherence wrappers preserve unknown daily fields. |
| Workout logging | `03:455-624,1887-2331`; metric/habit wrappers `04:1222-1734,2134-2135` | `populateWoDaySelect`, `renderWoExercises`, `collectWoData`, `getTodayWoData`, `p85ExecuteSave` | `woStatus`, `logGym`, progression helpers | `wo-*`, `lgbtn-*`, `woDaySelect`, `woExerciseLog`, review card | `D`, `W`, lifecycle, overrides → `D`, `W`, draft | Resolved program, recommendations, progression; History/export/Stats call stored result | Inline + cross-file | High | `W` retains `{dayIdx,gym,exercises:{id:{sets:[{wt,reps,rir}]}}}` compatibility. |
| Workout History | entry `03:2332-2335`; filters/render `03:4228-4386`; habit wrapper `04:2131-2132`; basketball wrapper/UI `05:272-305,377-380` | `renderHistory`, `p7GetAllEntries`, `p7ApplyFilters`, `renderHistoryFromEntries`, `mfBasketballRenderHistory` | `p7FilterState` | `screen-history`, `hf-*`, `histList`, `mfBasketballHistory` | `D`, `W`, habit definitions, basketball → none | Daily/workout/habits/basketball; `showScreen` and filter listeners call it | Inline/cross-file wrapper target | High | Wrapper order is base → habits → basketball. DOM card indexing couples habit append to base renderer. |
| Stats/analytics | `03:4387-4663`; adherence wrapper `04:1977`; habit calculation wrapper `04:2128-2129`; basketball wrapper/UI `05:306-319,381-383` | `p7CalcAnalytics`, `p7RenderAnalytics`, `p9510RenderStats`, `mfBasketballRenderStats` | analytics helpers | `screen-analytics`, `p7-analytics-content`, `p9510StatsSummary`, `mfBasketballStats` | `D`, `W`, habits, recurring events, basketball → none | History-shaped data, adherence, habits, basketball; navigation calls renderer | Cross-file wrapper target | High | Preserve calculation wrapper before render wrappers: habit calc → adherence render → basketball render. |
| Progression metrics/review | base `03:471-1325,2012-2233`; corrections/wrappers `04:1222-1734` | `p9GetExerciseHistory`, parse/format/suggestion/status/export functions, `p949BuildWorkoutReview`, `p959*` | metric tables/helpers | workout exercise cards, `p949Review*`, diagnostics `p945*` | `W`, resolved program/overrides → none | Workout render/review, rotation export, Stats/debug; 9.5.9 reassigns many base functions | Inline/debug/cross-file | High | Load direction and assisted/bodyweight/duration semantics are compatibility contracts. |
| Program proposal engine | `02:2132-3940` | `p954NormalizeProposal`, validate/build/generate/save/apply/undo functions, `p955*` context/quality | `PROGRAM_PROPOSAL_KEY`, proposal schemas/enums | indirect proposal UI | proposal, profile, onboarding, prefs, `LC`, overrides, logs → proposal, `LC`, overrides | Onboarding/export/debug/UI; AI Sync remains separate | Debug + cross-file | High | Apply/undo is a raw-storage transaction; unsupported lifecycle actions stay deferred. |
| Proposal review UI | `02:3940-5222`; starter extension `04:1219-1220` | `p954RenderProgramPersonalization`, `p954R*` review/apply/undo handlers | UI state | `p954Container`, review/confirm/apply/undo overlays | proposal/profile/onboarding → proposal and apply/undo stores | Proposal engine, starter chooser, `showScreen`, inline handlers | Inline requirement | Medium | Closely coupled stateful overlay; defer handler migration. |
| Onboarding | `02:408-2130`; boot `04:2208-2216` | `p951*` state/freshness, `p952*` UI, `p953*` answer application | `ONBOARDING_KEY`, schemas, UI state | `p952*` overlays/step container | onboarding, profile, prefs, daily/lifecycle evidence → onboarding/profile/prefs | Proposal generation, first-sync, boot | Inline + debug | High | Fresh-install detection must distinguish seeded baseline from meaningful user data. |
| Starter-program selection | `04:830-1221`; boot guard `04:2221-2225` | template registry, `getActiveProgramBasis`, `getActiveBaseProgram`, chooser/render/confirm | `P958_*`, `starterProgramStateReady` | dynamically built `p958*`, program screen | profile → profile only | `P`, profile, resolved rendering, export, personalization, boot | Public/debug/cross-file | High | Snapshots `JSON.stringify(P)`; selection rollback restores profile raw bytes. |
| Profiles/personalization | profile `02:101-406`; program card `02:4214-4448`; starter `04:975-1221` | `p950*`, `p954RenderProgramPersonalization`, basis helpers | `USER_PROFILE_KEY`, schema | `p950*`, `p954Container` | user profile → user profile | Onboarding, export, starter, proposal, boot | Inline/debug/cross-file | Medium | Live body weight stays in daily logs; `programBasis` is optional profile state. |
| AI coaching preferences | `02:19-99` | `p9Get/Save/Render/Reset/ClearCoachPrefs` | `AI_PREFS_KEY`, starter template | `coachPrefsTa/Result` | prefs → prefs | AI export, onboarding/proposal context, backup, boot | Inline/cross-file | Low | Plain text schema; preserve exact export placement and default template. |
| Personalized habits | legacy data/UI `01:16-121`; active implementation `04:1981-2189` | `p960*` normalize/store/render/manage/analyze/proposal; reassigns `initHabitState`, `renderHabits`, analytics/history/save/export/Sync | habit key constants; `habitState`; manager draft state | `habitCards`, manager/proposal DOM, History/Stats | definitions, proposal, `D` → same | Daily save, History, Stats, export, AI Sync; calls backup allowlist | Inline/debug/cross-file | High | Active code deliberately preserves seven legacy IDs and unknown daily habit records. |
| Schedule-aware adherence | `04:1736-1979` | `p9510*` schedule/event/occurrence/UI/export/debug | recurring key constants | `p9510*` setup/actions/stats | recurring items/events and legacy `D.zep` → recurring stores and daily bridge | Daily load/toggles, Stats, export, restore diagnostics | Inline/debug/cross-file | High | Structured events outrank legacy daily evidence; wrapper chain must remain before habits/basketball. |
| AI export | base `03:2337-3258`; starter/metric/adherence/habit wrappers `04:1162-1175,1723-1734,1978,2138-2139`; basketball `05:326-343,390-399` | `getExportDkeys`, `buildLogSection`, `p9489*`, `p957*`, `p955BuildProposalExport`, `genExport`, `doCopy` | `window._exp`, export range state | `exportRangeSelect`, `exportMeta`, `exportOut`, `copyBtn` | all read models → none | Program/profile/proposal/progression/habits/adherence/basketball; invoked inline | Inline + shared export buffer | Critical | Effective chain: base → starter → metrics → adherence → habits → basketball. Marker placement is contractual. |
| AI Sync/import | base `03:3260-4067`; habit wrapper `04:2163-2164` | `applySync` and nested validators/preprocessors; habit import wrapper | parser constants/local plan | `syncInput`, `syncResult` | lifecycle, overrides, recommendations, proposal/habit state → supported stores | Lifecycle/resolved program, proposals, habits; inline invokes | Inline + wrapper target | Critical | Ordered plan, validation, rollback, ID stability, and invalid-input rejection must remain identical. Basketball is intentionally absent. |
| Backup creation | `04:4-28,171-180`; basketball allowlist wrapper `05:351-355` | `p8IsMarcusFitKey`, `p8GetMarcusFitKeys`, `p8BuildBackup`, `p8CreateBackup` | `SCHEMA_VERSION` | `p8BackupTa/Result` | all owned keys → none | Every storage owner; basketball wraps allowlist | Inline/cross-file wrapper target | Critical | Serializes raw strings, not parsed objects. Unknown owned keys are excluded unless allowlisted. |
| Backup preview/validation | `04:30-286`; basketball wrappers `05:356-375` | `p8492SummarizeBackup`, format, `p8ValidateBackup`, migrate, restore-preview handlers | `p8PendingRestoreBackup` | backup textarea and confirm panel/body | pasted backup only → none | Restore; basketball wraps summary/format/validation | Inline/cross-file wrapper targets | High | Old backups may omit optional keys; summary recognition and validation are separate concerns. |
| Restore/clear | `04:281-348`; post-checks `01:1633-1648`, `04:1953-1958` | `p8ExecuteRestore`, confirm/cancel/clear, `mfRunPostRestoreValidation` | pending restore | backup result/confirm panels | all current owned keys → delete all then restore allowed backup keys | Backup ownership, lifecycle/adherence diagnostics, reload | Inline/cross-file | Critical | Replacement semantics intentionally remove current optional keys absent from an old backup. Failure safety copy is in-memory/UI only. |
| Debug helpers | `01:1150-1732`; `02:937-999,2045-2129,3191-3939,5166`; `03:1098-1833,2219-2232,2817-2822`; `04:360-826,1185-1217,1622-1721,1951-1971,2172-2186`; `05:401-405` | all `mf*Debug`, audits, self-tests | explicit `window.*` exports | diagnostics plus console | broad reads; self-tests temporarily write then restore | Used by tests/manual QA/console | Debug-only public | Medium | Do not privatize without replacing tests/manual QA contracts; distinguish read-only debug from self-tests. |
| Navigation/screen switching | gym/program `03:100-470`; screens `04:2191-2199`; basketball wrapper `05:384-389` | `setGym`, `setLogGym`, `renderProgram`, `showScreen`, `updateTrackerDate` | `gym`, `logGym`, `tDate` | tabs, screens, gym controls, date label | profile/program/daily reads → draft through interactions | All render systems call/are called by navigation | Inline + wrapper target | High | `showScreen` is defined near final boot then wrapped by basketball; screen calls are render hooks. |
| Shared UI utilities | `03:4069-4226` plus feature-local DOM helpers | `p6Toggle`, badge/sticky helpers, save hooks/listeners | timers/observers | `p6sec-*`, badges, sticky bar | form/draft → draft through wrappers | Daily/workout/basketball; inline attributes and load listeners | Inline/cross-file | Medium | Three `window.load` listeners and MutationObserver behavior come from accepted source order. |
| Basketball logging | `05:7-271,421-429` inside IIFE | normalize/read/write/save/delete/form/init functions | private basketball constants/state | `mfBasketball*` log form/dialog/badge | basketball key → basketball key | `tDate`; History/Stats/export/backup wrappers | Only debug public; event listeners internal | Low | Isolated schema v1, stable `bball-*` IDs, multiple same-day sessions; no lifting/AI Sync changes. |
| Basketball History/Stats/export/backup | `05:272-405` | render/aggregate/export/backup validators and late wrappers | `window._exp`, `mfBasketballDebug` | basketball History/Stats containers | basketball key and backup data → none | Base History/Stats/navigation/export/backup functions | Debug public; wrappers cross-file | Medium | Must remain last so it captures final accepted implementations. |
| Boot/initialization | evaluation side effects across files; final `04:2200-2226`; basketball `05:421-429` | store initializers, renders, `mfBasketballInit` | shared initialized state | assumes all HTML parsed | multiple stores read/initialized | Every feature; invoked by script evaluation | Internal but order-critical | Critical | No `DOMContentLoaded`; `defer` evaluation and synchronous tail boot are intentional. |

## Verified dependency direction

```text
P + lifecycle/overrides
  -> resolved program
  -> program UI / workout logging / proposal engine / progression
  -> History + Stats + AI export

daily logs + workout logs
  -> progression / History / Stats / proposal context / AI export

profile + onboarding + preferences
  -> proposal engine / starter basis / AI export

backup ownership
  <- every storage owner
  -> preview -> validate -> migrate -> replacement restore -> validation -> reload

base History/Stats/export/backup
  -> adherence/habit wrappers
  -> basketball wrappers (last)
```

The proposed extraction must reverse none of these edges. Where two systems
currently interact through a shared classic-script binding, 10.1.2 may move the
definition but must keep an equivalent global binding until all callers and
inline attributes have a separately tested migration path.
